$kind: http-request
name: Activate Customer
method: PATCH
url: '{{baseUrl}}/api/v1/customers/{{customerId}}/activate'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
