$kind: http-request
name: Get Notifications By Customer
method: GET
url: '{{baseUrl}}/api/v1/notifications/customer/{{customerId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
