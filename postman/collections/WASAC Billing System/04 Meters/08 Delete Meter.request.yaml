$kind: http-request
name: Delete Meter
method: DELETE
url: '{{baseUrl}}/api/v1/meters/{{meterId}}'
order: 8000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200 or 204", function () {
        pm.expect(pm.response.code).to.be.oneOf([200, 204]);
      });
