$kind: http-request
name: Deactivate Meter
method: PATCH
url: '{{baseUrl}}/api/v1/meters/{{meterId}}/deactivate'
order: 7000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
