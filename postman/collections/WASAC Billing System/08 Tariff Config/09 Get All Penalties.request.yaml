$kind: http-request
name: Get All Penalties
method: GET
url: '{{baseUrl}}/api/v1/config/penalties'
order: 9000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
