$kind: http-request
name: Get All Fixed Charges
method: GET
url: '{{baseUrl}}/api/v1/config/fixed-charges'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
